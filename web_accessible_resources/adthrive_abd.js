// Redirect target for AdThrive's anti-adblock-detection ("abd") script. Its
// real API surface isn't well-documented enough to mimic with confidence, so
// this is a plain no-op — just a valid, harmless response instead of a
// failed load.
(function () {
  'use strict';
})();
