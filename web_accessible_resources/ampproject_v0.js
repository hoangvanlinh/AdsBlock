// Redirect target for AMP's v0.js runtime loader. The real runtime's API
// surface is far too large to mimic with confidence, so this is a plain
// no-op — just a valid, harmless response instead of a failed load.
(function () {
  'use strict';
})();
